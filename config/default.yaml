log_dir: /pub/srachaba/bubbleformer_logs
checkpoint_path: /share/crsp/lab/amowli/share/nucleus-ckpt-sid/neighbor_moe_exp_may13.ckpt
commit_sha:

seed: 42

nodes: 1
devices: 1

max_epochs: 1000
max_steps: 100000
val_check_interval: 100
batch_size: 16
accumulate_grad_batches: 1

history_time_window: 8
future_time_window: 8
time_step: 1
start_time: 100

defaults:
  - _self_
  - data_cfg: poolboiling64_gravity
  - model_cfg: nucleus2/nucleus2_pushforward
  - optim_cfg: adamw
  - scheduler_cfg: cosine_warmup
  - normalizer_cfg: gravity_std
